@page
@using App.UI.Configurations
@using SharedTenant.Domain
@model App.UI.Pages.process_paymentModel


<link href="/css/pages/print_style.min.css" rel="stylesheet" />
<div class="receipt-page">
    <div class="container">
        <h1 class="initial-title text-center mb-3 pb-1">
            <svg width="64" height="64" class="mb-3 d-block m-auto">
                <use xlink:href="/image/svg/master.svg#check"></use>
            </svg>
            Thank you this is details of your Order
        </h1>
        <p class="text-center">You will receive your order in two or three days .</p>

        <div class="receipt-content">
            <h2>Order Confirmation</h2>
            <div class="row">
                <div class="col-lg-3">
                    <img src="/image/log.svg" class="img-fluid">
                </div>
                <div class="col-lg-5">
                    <span><b>Booking Number:</b>@Model.CallBackData.Id</span>
                    <span><b>Booking Date:</b>@Model.CallBackData.CreatedAt</span>
                    
                </div>
                <div class="col-lg-4">
                    <span><b>Name:@Model.user.UserName</b></span>
                    <span><b>Phone:</b>@Model.user.PhoneNumber</span>
                    <span><b>Email:</b>@Model.user.Email</span>
                    <span><b>Address:</b>@Model.user.Address</span>
                </div>
            </div>
            <hr />
            <h2>Books Purchased</h2>
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Book Name</th>
                       @* <th scope="col">Amount</th>*@
                        <th scope="col">Price</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        
                        @foreach (var item in Model.cart.CartList)
                                   {
                        <td>@item.BookName</td>
                        <td>@item.BookPrice</td>
                           
                       }
                      @* @Model.cart.DeleteCartItemInSameBookStore(Global.UrlName,user.Id);*@
                    </tr>
                   <tr class="total-row">
                        <td></td>
                        <td></td>
                        <td>Total</td>
                        <td><b>@Model.CallBackData.AmountCents/100</b> EGP</td>
                    </tr>
                </tbody>
            </table>
           @* <hr class="mt-5" />
            <h2 class="mt-5 mb-4">Contact Info</h2>
            <div class="row">
                <div class="col-lg-6">
                    <span href="tel:+@Model.BusinessInfo.PhoneNumber"><b>Telephone:</b>@Model.BusinessInfo.PhoneNumber</span>
                    <span href="mailto: @Model.BusinessInfo.ContactUsEmail"><b>Email:</b>@Model.BusinessInfo.ContactUsEmail</span>
                </div>
                <div class="col-lg-6">
                    <span><b>Address:</b>@Model.BusinessInfo.Address</span>
                </div>
                <div class="col-12 text-center mt-5">
                    <button type="button" class="initial-btn" onclick="window.print()"><i class="fa-solid fa-print me-2"></i>Print</button>
                </div>
            </div>*@
        </div>
    </div>
    
</div>